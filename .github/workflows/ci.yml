name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

permissions:
  contents: read

jobs:
  build:
    name: Build, test & lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate Gradle wrapper
        uses: gradle/actions/wrapper-validation@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build, unit tests and Android Lint
        run: ./gradlew build

      - name: Static analysis (detekt)
        run: ./gradlew detekt

      - name: Coverage gate (Kover)
        run: ./gradlew koverXmlReport koverVerify

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: core-domain/build/reports/kover/report.xml,core-data/build/reports/kover/report.xml
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: true

      - name: Screenshot tests (Roborazzi)
        run: ./gradlew verifyRoborazziDebug
